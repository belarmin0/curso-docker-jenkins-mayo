pipeline {
    agent {
        kubernetes {
            defaultContainer ''
            yamFile 'agent-node.yaml'
        }
    }
    stages {
        stage('CI - pnpm') {
            stages
        }
    }
}